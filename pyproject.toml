[project]
name = "mc-execute"
version = "0.2.0"
description = "Simple python library that simulates Minecraft's execute command."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.1",
]
