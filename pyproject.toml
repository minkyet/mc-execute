[project]
name = "executor"
version = "0.1.0"
description = "Position & Rotation Calculator inside execute command in Minecraft."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.1",
]
