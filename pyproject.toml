[project]
name = "execute-playground"
version = "0.1.0"
description = "Simple python module that simulates Minecraft's execute command."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.4.1",
]
